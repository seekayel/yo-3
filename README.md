# starter-micro-api
should fail with 
```
FileNotFound: the file "index.js" is not found in the repository.
To fix, try one of the following:
        - make sure the file "index.js" exists and is pushed to GitHub
        - remove the "main" entry and create a "scripts" entry with a "start" script instead:
        "scripts": {
            "start": "node index.js"
          }
```