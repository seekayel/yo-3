{
  "name": "beder-server",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "dev": "nodemon ./bin/www",
    "prod": "NODE_ENV=production nodemon ./bin/www",
    "staging": "NODE_ENV=staging nodemon ./bin/www",
    "deploy": "git push heroku develop:main",
    "deploy-prod": "git push heroku-prod release:master"
  },
  "dependencies": {
        "lodash": "^4.17.21"
  },
  "devDependencies": {
    "moment": "^2.29.4"
  }
}
